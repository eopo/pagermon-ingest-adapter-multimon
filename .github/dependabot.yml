version: 2
updates:
  - package-ecosystem: 'npm'
    directory: '/'
    schedule:
      interval: 'weekly'
    target-branch: 'main'
    groups:
      npm-runtime:
        dependency-type: 'production'
      npm-tooling:
        dependency-type: 'development'
        patterns:
          - '*'
    open-pull-requests-limit: 10
    commit-message:
      prefix: 'deps(app)'
    labels:
      - 'dependencies'
      - 'dependabot'

  - package-ecosystem: 'docker'
    directory: '/'
    schedule:
      interval: 'weekly'
    target-branch: 'main'
    groups:
      docker-images:
        patterns:
          - '*'
    open-pull-requests-limit: 5
    commit-message:
      prefix: 'deps(docker)'
    labels:
      - 'dependencies'
      - 'dependabot'

  - package-ecosystem: 'github-actions'
    directory: '/'
    schedule:
      interval: 'weekly'
    target-branch: 'main'
    groups:
      github-actions:
        patterns:
          - '*'
    open-pull-requests-limit: 5
    commit-message:
      prefix: 'ci(deps)'
    labels:
      - 'dependencies'
      - 'dependabot'

# Reduce PR noise by grouping related dependency updates.
